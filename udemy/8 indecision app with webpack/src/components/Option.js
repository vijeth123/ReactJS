import React from 'react';
import ReactDOM from 'react-dom';

const Option = (props) => (
                              <div>
                                  {props.option}
                              </div>
                          );
export default Option;